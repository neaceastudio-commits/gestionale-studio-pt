# Server-side only. Keep false until the first real send is explicitly authorized.
WHATSAPP_AGENDA_ENABLED=false
WHATSAPP_AGENDA_SITE_ID=
WHATSAPP_SENDER_PHONE=+393520525230
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
# Select a supported Graph API version in Meta, e.g. vXX.0 (never credentials here).
WHATSAPP_API_VERSION=
WHATSAPP_TEMPLATE_LANGUAGE=it
# Approved body-only templates, indexed by exact appointment count.
WHATSAPP_AGENDA_TEMPLATES_JSON={}
# Existing server-side identity and database variables are reused:
# SUPABASE_SECRET_KEY or SUPABASE_SERVICE_ROLE_KEY
# PT_ACCESS_SECRET (or existing RESEND_API_KEY fallback)
